
### notes

* in Terminal 1, cd into `server` and build/run Docker image
    * observe `simple-python-server` image in Docker
    * confirm with http://localhost:8888
* in Terminal 2, run `./test.sh` from this folder
